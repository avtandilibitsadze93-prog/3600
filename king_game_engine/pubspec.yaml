name: king_game_engine
description: "Shared rules engine for the King (კინგი) card game — pure Dart, no Flutter dependency."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
